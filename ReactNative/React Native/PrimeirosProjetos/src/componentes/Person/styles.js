import {StyleSheet} from "react-native";

const styles = StyleSheet.create({  
    container: {
        width:  '100%',
        padding: 10,

    },

    texto: {
        fontSize: 20,
        color: '#ffff',
    },
});

export default styles;